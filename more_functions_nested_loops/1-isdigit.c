#include <stdio.h>
#include "main.h"

/**
 * _isdigit - check the code
 * @c: value to check
 * Description: fvgbsdf
 * Return: 1 if uppercase else 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);
	return (0);
}
